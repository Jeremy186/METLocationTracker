@model MapsProposal.Models.AdminViewModel

@{
    ViewBag.Title = "Admin";
}

<h2>Users</h2>
<table>
    @foreach (var user in Model.Users)
    {

        <tr>
            <td>@user.Email</td>
            <td>@user.FirstName</td>
            <td>@user.LastName</td>
        </tr>

    }
</table>